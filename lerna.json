{
	"version": "0.0.0",
	"npmClient": "npm",
	"packages": ["packages/*"],
	"command": {
		"version": {
			"allowBranch": "monorepo",
			"message": "chore(release): %s"
		}
	},
	"ignoreChanges": ["**/cypress/**"]
}
