# Node definition for communication node
---
id: 0x20 # 4 leading bits subsystem id , 4 bits node id
name: comms
apps:
  # global apps (for all nodes)
  housekeeper: default
  boot-manager: default
  data-upload: default
  watchdog: default
  # node specific apps
  commander: default # with timed command manager
  downlink-manager: default
  # sun-sensor-pwctrl: default
