import {
	Meta,
	Story,
	Canvas,
	ArgsTable,
	Description
} from '@storybook/addon-docs/blocks';
import { CommonWrapper } from '../../common-wrapper';
import { Header } from '../header';
import { TelestionClient, useAuth } from '@wuespace/telestion-client-core';
import { MemoryRouter } from 'react-router';
import { DashboardPicker } from './dashboard-picker';
import { useUserConfig } from '../../../hooks';

<Meta
	title="Common/Header/Dashboard Picker"
	component={DashboardPicker}
	decorators={[
		Story => {
			useAuth.setState({ auth: { username: 'alice' } });
			useUserConfig.setState({
				userConfig: {
					alice: {
						dashboards: [{ title: 'Dashboard A' }, { title: 'Dashboard B' }]
					}
				}
			});
			return <TelestionClient>{Story()}</TelestionClient>;
		}
	]}
	argTypes={{}}
/>

# Dashboard Picker

<Description of={DashboardPicker} />

## Importing

```tsx
import { DashboardPicker } from '@wuespace/telestion-client-common';
```

## Examples

<Canvas>
	<Story
		height={'256px'}
		name="Simple Dashboard picker"
		args={{}}
		inline={false}
	>
		{args => (
			<MemoryRouter initialIndex={0}>
				<CommonWrapper>
					<Header center={<DashboardPicker {...args} />} />
				</CommonWrapper>
			</MemoryRouter>
		)}
	</Story>
</Canvas>

## Properties

n/a
